{
  "id": "24575bb5",
  "title": "[Query Opt] Optimize app detail page queries",
  "tags": [
    "backend",
    "performance",
    "database"
  ],
  "status": "open",
  "created_at": "2026-02-28T17:51:18.210Z"
}

Parent: Epic: Database Query Optimization

## Current State (app/apps/[slug]/page.tsx)
7+ queries via Promise.all:
1. getCurrentUser
2. getAppMedia
3. getLatestUpdates
4. getAppReviews
5. getAppStoreReviews
6. getCombinedReviewStats
7. getArticleCounts

## Optimization Options

### Option A: Batch related queries
```typescript
const [mediaAndUpdates, reviewData] = await env.DB.batch([
  // App data query (media + updates in one)
  env.DB.prepare(`...`),
  // Review data query (reviews + stats in one)
  env.DB.prepare(`...`),
]);
```

### Option B: Single mega-query with JSON aggregation
SQLite supports JSON functions - aggregate related data.

## Tasks
1. Identify which queries can be combined
2. Test D1 batch API performance
3. Implement optimized queries
4. Benchmark before/after
