{
  "id": "85e401af",
  "title": "🟡 P1: Add admin CRUD operations tests",
  "tags": [
    "testing",
    "p1"
  ],
  "status": "done",
  "created_at": "2026-02-27T22:32:45.149Z"
}

## Completed

Created comprehensive admin CRUD operation tests across 3 new files with 47 tests total:

### Files Created

**`tests/integration/admin-apps.test.ts` (14 tests)**
- GET /api/admin/apps: auth required, non-admin rejected, list apps, API key auth
- POST /api/admin/apps: auth required, create app, duplicate slug, validate name/slug/tagline/platforms, optional fields
- Edge cases: missing DB config, invalid JSON

**`tests/integration/admin-versions.test.ts` (14 tests)**  
- POST /api/admin/versions/presign: auth required, return R2 key, key format, required fields, file type validation (.zip/.dmg only), filename sanitization
- POST /api/admin/versions: auth required, create version, required fields, version format (x.y.z), non-existent app, optional fields (signature, releaseNotes)
- Edge cases: missing DB config, API key auth

**`tests/integration/admin-codes.test.ts` (19 tests)**
- GET /api/admin/codes: auth required, non-admin rejected, list codes, API key auth
- POST /api/admin/codes: auth required, create percent code, create fixed code, uppercase conversion, validate code length, validate discount_type, reject percent > 100, reject zero discount, reject duplicate, app-specific codes, max_uses, expires_at
- Edge cases: missing DB config, invalid JSON, API key auth

### Test Patterns Used
- vi.mock for cloudflare-context and auth/middleware
- vi.resetModules() for test isolation
- fixtures for test data (testUser, adminUser, testApp, etc.)
- API key authentication tested alongside session auth
- Error handling and validation edge cases

All 47 tests passing.
