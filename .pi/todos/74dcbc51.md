{
  "id": "74dcbc51",
  "title": "🟡 P1: Add CLI command tests",
  "tags": [
    "testing",
    "p1",
    "cli"
  ],
  "status": "done",
  "created_at": "2026-02-27T22:32:45.150Z"
}

## Context
The CLI (`isolated` command) is critical for developer experience. Commands like `publish`, `login`, and `whoami` need testing.

## ✅ Completed

### Test Infrastructure
- Created `cli/vitest.config.ts`
- Added `test` and `test:run` scripts to `cli/package.json`
- Created `cli/tests/mocks/index.ts` with:
  - `createMockFetch()` - Mock fetch for API tests
  - `createMockFS()` - Mock filesystem for config tests
  - `createMockExec()` - Mock child_process for Xcode/Sparkle tests
  - `mockEnv()` - Environment variable mocking helper
  - `fixtures` - Test data objects

### Library Tests (86 tests)
1. **`cli/tests/lib/config.test.ts`** - 19 tests
   - getConfig, saveConfig, getCredentials, saveCredentials
   - clearCredentials, isAuthenticated, getToken
   - API_URL environment variable handling

2. **`cli/tests/lib/api.test.ts`** - 18 tests
   - Request methods (GET, POST), error handling
   - Auth endpoints (initiateDeviceAuth, pollDeviceAuth)
   - App endpoints (listApps, getApp, registerApp)
   - Version endpoints (listVersions, getPresignedUploadUrl, confirmUpload)
   - URL construction and API key headers

3. **`cli/tests/lib/xcode.test.ts`** - 18 tests
   - detectXcodeProject (xcodeproj, xcworkspace, version extraction)
   - findReleaseZip (most recent, prefer *-macOS.zip)
   - parseChangelog (version extraction, brackets handling)
   - deriveSlug (bundle ID to slug, camelCase, special chars)
   - bumpBuildNumber (agvtool integration)

4. **`cli/tests/lib/sparkle.test.ts`** - 14 tests
   - canSign (tools, env, keychain, file)
   - signForSparkle (tools, native Ed25519 fallback)
   - getSigningInfo (method and source detection)
   - DerivedData tool discovery

5. **`cli/tests/lib/output.test.ts`** - 17 tests
   - setJsonMode/isJsonMode
   - output, success, error, warn, info
   - banner, kv
   - JSON vs text mode output

### Command Tests (20 tests)
6. **`cli/tests/commands/whoami.test.ts`** - 5 tests
   - Unauthenticated error handling
   - Display user info, API endpoint call
   - Invalid token handling, API key header

7. **`cli/tests/commands/logout.test.ts`** - 5 tests
   - Not logged in message
   - Credential clearing, success message
   - JSON mode output

8. **`cli/tests/commands/apps.test.ts`** - 10 tests
   - apps list: auth check, list display, JSON output, empty list, errors
   - apps versions: auth check, version display, JSON output, API endpoint

## Test Results
- **CLI Tests:** 106 tests passing (8 test files)
- **Website Tests:** 232 tests passing (14 test files)
- **Total:** 338 tests passing

## Remaining Work (Future)
- Test login command (requires browser mock for open package)
- Test publish command (complex flow with multiple API calls)
- Test init command
- Test doctor/status commands
