{
  "id": "3b0d5994",
  "title": "Add tests for CLI login command",
  "tags": [
    "testing",
    "important",
    "cli"
  ],
  "status": "done",
  "created_at": "2026-02-28T05:46:25.699Z"
}

## Completed
Created `cli/tests/commands/login.test.ts` with 21 tests covering:

### Already authenticated (4 tests)
- Exit early when already logged in with valid token
- Proceed with login when token is invalid
- Re-authenticate when --force flag is used
- JSON output when already authenticated

### Auth initiation (4 tests)
- Fail when initiate auth fails
- Display verification code and URL
- Open browser with verification URL
- Continue even if browser fails to open

### Polling (5 tests)
- Poll until complete
- Save credentials on success
- Fail when auth expires
- Send correct device code in poll request
- Save userId on success

### JSON mode (2 tests)
- Output JSON on success
- Output JSON message on authentication success

### Error handling (3 tests)
- Handle empty initiate response
- Show error on network failure during initiate
- Handle missing user data on complete

### Output messages (3 tests)
- Display success message with email
- Display verification URL
- Display already logged in message

## Technical Notes
- Tests use real timers (2s poll interval) so tests take ~32s total
- Mocks: fs, os, fetch, open, ora spinner
- Browser opening failure doesn't break the flow - login continues
