{
  "id": "c5a38c36",
  "title": "🟡 P1: Add comprehensive E2E tests",
  "tags": [
    "testing",
    "p1",
    "e2e"
  ],
  "status": "done",
  "created_at": "2026-02-27T22:33:36.078Z"
}

## Context
E2E tests verify complete user flows in a real browser. Previously only minimal coverage existed.

## Implementation Complete ✅

### Test Helpers Created
- `tests/e2e/helpers/auth.ts` - Authentication helpers (loginAsUser, loginAsAdmin, logout)
- `tests/e2e/helpers/seed.ts` - Test data seeding helpers (seedAll, cleanupTestData)
- `tests/e2e/helpers/index.ts` - Re-exports all helpers

### Test API Endpoints Created
- `app/api/test/auth/route.ts` - Test authentication endpoint (dev mode only)
- `app/api/test/seed/route.ts` - Test data seeding endpoint (dev mode only)

### Test Files Created (112 E2E tests total)
1. **`tests/e2e/auth-flow.spec.ts`** - 14 tests
   - Login page display and validation
   - Magic link flow
   - Social login buttons
   - Session persistence
   - Sign out functionality
   - Protected route redirects

2. **`tests/e2e/purchase-flow.spec.ts`** - 20 tests
   - Homepage app display
   - App detail page navigation
   - Pricing display (free vs paid)
   - Purchase prompts for unauthenticated users
   - Discount code UI
   - Error handling (404, network errors)

3. **`tests/e2e/dashboard-flow.spec.ts`** - 14 tests
   - Unauthenticated access handling
   - Dashboard tabs (My Apps, Reviews, Settings)
   - Empty state display
   - Navigation between sections

4. **`tests/e2e/admin-flow.spec.ts`** - 26 tests
   - Access control (unauthenticated, non-admin, admin)
   - Dashboard stats display
   - Apps management (list, create, edit)
   - Discount codes management
   - Users management
   - Purchases and downloads
   - Feedback management

5. **`tests/e2e/feedback-flow.spec.ts`** - 19 tests
   - Public feedback page
   - Stats display
   - Submit functionality (authenticated)
   - Roadmap page
   - Legal pages (terms, privacy)
   - Navigation integration

6. **`tests/e2e/responsive.spec.ts`** - 19 tests
   - Mobile, tablet, and desktop viewports
   - Visual consistency across pages
   - Accessibility checks (headings, alt text, labels, keyboard nav)
   - Performance checks (page load times)

### Features
- Test isolation with beforeAll/afterAll hooks for seeding
- Proper cleanup of test data
- Supports both user and admin authentication
- Works with dev server (npm run dev)
- Screenshots on failure (configured in playwright.config.ts)

### Test Commands
```bash
# Run all E2E tests
npm run test:e2e

# Run specific test file
npx playwright test tests/e2e/auth-flow.spec.ts

# Run with UI
npx playwright test --ui

# Run headed (see browser)
npx playwright test --headed
```

## Acceptance Criteria
- [x] All critical user flows covered
- [x] <30s per test (individual tests are quick)
- [x] Screenshots on failure (configured)
- [ ] Tests pass in CI (needs CI to be set up with test data)

## Notes
- Tests require dev server running (`npm run dev`)
- Test auth endpoint only works in development mode
- Some tests are skipped pending purchase seeding implementation
