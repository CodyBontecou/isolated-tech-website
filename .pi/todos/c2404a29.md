{
  "id": "c2404a29",
  "title": "Add tests for App Store Connect integration (lib/app-store-connect.ts)",
  "tags": [
    "testing",
    "critical",
    "integration"
  ],
  "status": "closed",
  "created_at": "2026-02-28T05:46:25.652Z"
}

## Completed

Created `tests/unit/app-store-connect.test.ts` with 21 tests covering:

### JWT Generation (5 tests)
- Valid JWT structure with three parts (header.payload.signature)
- Correct JWT header with ES256 algorithm, type, and kid
- Correct JWT payload claims (iss, aud, iat, exp with 20-minute validity)
- ES256 (ECDSA P-256) signing with SHA-256
- Token caching and reuse within validity period

### getReviews (8 tests)
- Successful review fetching
- Review normalization with null fields preserved
- Correct API endpoint and parameters (limit, sort)
- Pagination handling with next links
- maxPages limit enforcement
- API error handling with status code
- Empty response handling
- Default sort order (-createdDate)

### getReviewsSince (3 tests)
- Date filtering for recent reviews
- Empty array when no reviews after date
- High limit and page settings for comprehensive fetch

### getAppStoreConnectClient (5 tests)
- Client creation with valid env vars
- Missing KEY_ID throws error
- Missing ISSUER_ID throws error
- Missing PRIVATE_KEY throws error
- Escaped newlines in private key handling
